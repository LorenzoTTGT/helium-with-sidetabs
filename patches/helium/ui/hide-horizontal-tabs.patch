--- a/chrome/browser/ui/views/frame/top_container_view.cc
+++ b/chrome/browser/ui/views/frame/top_container_view.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/views/frame/top_container_view.h"

 #include "chrome/browser/ui/views/frame/browser_view.h"
+#include "chrome/browser/ui/tabs/features.h"
 #include "ui/base/metadata/metadata_impl_macros.h"
 #include "ui/views/layout/fill_layout.h"

@@ -20,6 +21,13 @@ TopContainerView::~TopContainerView() = default;
 const char* TopContainerView::GetClassName() const {
   return "TopContainerView";
 }
+
+void TopContainerView::OnPaint(gfx::Canvas* canvas) {
+  // Don't paint if vertical tabs are enabled
+  if (tabs::IsVerticalTabsFeatureEnabled()) {
+    return;
+  }
+  views::View::OnPaint(canvas);
+}

 BEGIN_METADATA(TopContainerView)
 END_METADATA
--- a/chrome/browser/ui/views/frame/top_container_view.h
+++ b/chrome/browser/ui/views/frame/top_container_view.h
@@ -24,6 +24,7 @@ class TopContainerView : public views::View {

   // views::View:
   const char* GetClassName() const override;
+  void OnPaint(gfx::Canvas* canvas) override;

  private:
   const raw_ptr<BrowserView> browser_view_;
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -1155,6 +1155,12 @@ void BrowserView::UpdateDevToolsForContents(
 }

 bool BrowserView::ShouldDrawTabStrip() const {
+  // When vertical tabs are enabled, don't draw the horizontal tab strip
+  if (tabs::IsVerticalTabsFeatureEnabled() && GetIsNormalType()) {
+    return false;
+  }
+
+  // Original logic for horizontal tabs
   // Make sure the tabstrip is visible when there is a tabstrip_model
   // available. It is also possible for the delegate to override to hide since
   // callers may otherwise try to access it. Note that we can't just check
--- a/chrome/browser/ui/views/tabs/tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/tabs/tab_strip_region_view.cc
@@ -10,6 +10,7 @@
 #include "chrome/browser/ui/browser_commands.h"
 #include "chrome/browser/ui/layout_constants.h"
 #include "chrome/browser/ui/tabs/features.h"
+#include "chrome/browser/ui/tabs/tab_style.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/views/chrome_layout_provider.h"
 #include "chrome/browser/ui/views/frame/browser_view.h"
@@ -65,6 +66,23 @@ TabStripRegionView::TabStripRegionView(BrowserView* browser_view)

   std::unique_ptr<TabStrip> tab_strip =
       std::make_unique<TabStrip>(std::move(tab_strip_controller));
+
+  // Configure tab strip for vertical layout
+  if (tabs::IsVerticalTabsFeatureEnabled()) {
+    // Set vertical orientation
+    SetLayoutManager(std::make_unique<views::BoxLayout>(
+        views::BoxLayout::Orientation::kVertical,
+        gfx::Insets::VH(8, 0),  // Add vertical padding
+        0  // No spacing between tabs
+    ));
+
+    // Set background for vertical tab strip
+    SetBackground(views::CreateThemedSolidBackground(kColorToolbar));
+
+    // Add a border to separate from content
+    SetBorder(views::CreateThemedSolidSidedBorder(
+        gfx::Insets::TLBR(0, 0, 0, 1), kColorToolbarSeparator));
+  }
+
   tab_strip->SetAvailableWidthCallback(
       base::BindRepeating(&TabStripRegionView::GetTabStripAvailableWidth,
                           base::Unretained(this)));
